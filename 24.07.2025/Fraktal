m = 10**9 + 7

def inv(x):
    return pow(x, m - 2, m)

n = int(input())
o = (n + 1) // 2
e = n // 2
p = pow(12, o, m)
q = pow(12, e, m)
i = inv(11)

a = (p - 1 + m) * i % m
b = (q - 1 + m) * 3 * i % m

print((a + b) % m)
